#!/usr/bin/env bash

LANG=C
d=$(date) && echo "${d}: Hello,"; sleep 2
d=$(date) && echo "${d}: ."; sleep 2
d=$(date) && echo "${d}: ."; sleep 2
d=$(date) && echo "${d}: ."; sleep 2
d=$(date) && echo "${d}: world!"
